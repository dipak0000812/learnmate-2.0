version: '3.8'

services:
  backend:
    build: ./learnmate-backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=1h
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
    restart: always

  ai-service:
    build: ./AI-Model
    ports:
      - "5001:5001"
    environment:
      - PORT=5001
      - MONGO_URI=${MONGO_URI}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always

  # Optional: Simple frontend serve (assuming static build)
  # frontend:
  #   image: nginx:alpine
  #   volumes:
  #     - ./learnmate-frontend/build:/usr/share/nginx/html
  #   ports:
  #     - "80:80"
